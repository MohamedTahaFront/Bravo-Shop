function NotFoundPage() {
  return (
    <div className="full">
      <b className="h2">
        Sorry, this page does not exist! It looks like you typed the link
        incorrectly or the page has been moved.
      </b>
    </div>
  );
}

export default NotFoundPage;
